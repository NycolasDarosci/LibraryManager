#ifndef ALG_II_BOOK_H
#define ALG_II_BOOK_H

#include <string>
using namespace std;

struct Book {
    string title;
    string author;
    int year;
    int copies;
};

void insert(Book book);
Book [] list()
#endif
